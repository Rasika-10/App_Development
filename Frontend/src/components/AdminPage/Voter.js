import React from 'react';
import Sidebar from '../sidebar';

const Voter = () => {
    return (
        <Sidebar>
        <div>
            <h1>Voter page</h1>
        </div>
        </Sidebar>
    );
};

export default Voter;